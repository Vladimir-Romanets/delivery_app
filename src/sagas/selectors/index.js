export const getPageLimit = ({ pagination }) => pagination.limit

export const getCurrentPageNumber = ({ pagination }) => pagination.currentPage

export const searchForm = ({ form }) => form.searchForm

export const getCheckedList = ({ masterTable }) => masterTable.checkedList